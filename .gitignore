node_modules/
build/

# As plataformas nativas geradas pelo Capacitor ficam na RAIZ do repositorio e
# continuam ignoradas. As regras abaixo sao ancoradas com "/" justamente para
# nao engolir o codigo nativo dos plugins locais em plugins/*/android e
# plugins/*/ios, que precisa estar versionado para o `npx cap sync` do CI.
/android/
/ios/

.env
.env.local
.env.production

*.keystore
*.jks
*.p12
*.mobileprovision
ExportOptions.plist
